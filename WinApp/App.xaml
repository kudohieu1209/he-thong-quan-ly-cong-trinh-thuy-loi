<Application x:Class="WinApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WinApp"
             xmlns:vst="clr-namespace:Vst.Controls"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="AppBackgroundBrush" Color="#F5F5F7" />
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="SurfaceAltBrush" Color="#F9FAFB" />
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#1D1D1F" />
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#6E6E73" />
        <SolidColorBrush x:Key="AccentBrush" Color="#007AFF" />
        <SolidColorBrush x:Key="AccentSoftBrush" Color="#E5F1FF" />
        <SolidColorBrush x:Key="BorderSoftBrush" Color="#DADCE0" />

        <Style TargetType="{x:Type Control}">
            <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
        </Style>

        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        </Style>

        <Style TargetType="{x:Type Label}">
            <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
        </Style>

        <Style TargetType="vst:Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderRadius" Value="9" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="MinWidth" Value="72" />
            <Setter Property="Padding" Value="12 7" />
            <Setter Property="Margin" Value="4 0" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0A84FF" />
                    <Setter Property="BorderBrush" Value="#0A84FF" />
                </Trigger>
            </Style.Triggers>

        </Style>
        <Style TargetType="vst:MenuButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="BorderRadius" Value="10" />
            <Setter Property="Margin" Value="4 0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="12 7" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                    <Setter Property="Background" Value="{StaticResource AccentSoftBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="vst:SideMenu">
            <Setter Property="Margin" Value="0 10 0 18" />
            <Setter Property="Background" Value="Transparent" />
        </Style>
        <Style TargetType="vst:SideMenuCaption">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="8 6 8 8" />

        </Style>
        <Style TargetType="vst:SideMenuButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="BorderRadius" Value="10" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="12 8" />
            <Setter Property="Margin" Value="0 2" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                    <Setter Property="Background" Value="{StaticResource AccentSoftBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="vst:VerticalMenu">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="20 22" />
        </Style>
        <Style TargetType="vst:TableHeaderCell">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}" />
            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}" />
            <Setter Property="BorderThickness" Value="0 0 0 1" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoftBrush}" />
            <Setter Property="Padding" Value="12 8" />
        </Style>
        <Style TargetType="vst:TableCell">
            <Setter Property="BorderThickness" Value="0 0 0 1" />
            <Setter Property="BorderBrush" Value="#EEF0F2" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="Padding" Value="12 8" />
        </Style>
        <Style TargetType="vst:TableView">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="RowHeight" Value="38" />
            <Setter Property="RowIndexWidth" Value="42" />
            <Setter Property="RowIndexBackground" Value="{StaticResource SurfaceAltBrush}" />
            <Setter Property="RowOverBackground" Value="{StaticResource AccentSoftBrush}" />
        </Style>
        <Style TargetType="vst:EditorFrame">
            <Setter Property="Padding" Value="12 9" />
            <Setter Property="BorderRadius" Value="10" />
            <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoftBrush}" />
            <Style.Triggers>
                <Trigger Property="Activated" Value="True">
                    <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="vst:EditorLabel">
            <Setter Property="Margin" Value="4 0 4 6" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}" />
        </Style>
        <Style TargetType="vst:Placeholder">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}" />
            <Setter Property="Opacity" Value="0.65" />
        </Style>
        <Style TargetType="vst:FormControl">
            <Setter Property="Padding" Value="6 0 6 18" />
        </Style>
    </Application.Resources>
</Application>
